
server {
    listen 80;
    server_name app-demo.localhost;

    location / {
        proxy_pass http://host.docker.internal:3005;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
